# UI/UX 가이드라인: TeamRoom

본 문서는 `PRD.md`의 요구사항을 기반으로 TeamRoom의 UI/UX 설계 방향을 정의한다.
개발자 서브에이전트는 이 문서를 코드 구현의 1차 참고 기준으로 삼는다.

---

## 1. 디자인 컨셉 요약

**한 줄 컨셉: "군더더기 없는 팀 커맨드 센터" — 3~10명이 접속 즉시 상황을 파악하고 바로 소통을 시작할 수 있는, 채팅·공지·코드 협업이 한 화면에 정리된 명료한 워크스페이스.**

### 레퍼런스 근거
- **Slack / Discord (3-Column 레이아웃)**: 두 서비스 모두 "좌측 채널 목록 - 중앙 콘텐츠 - 우측 멤버/부가정보" 구조로 수렴했다. PRD에 명시된 "좌측 채팅방 목록 / 중앙 메인 / 우측 대시보드" 구조는 이미 사용자에게 학습된 멘탈모델과 일치하므로, 새로운 레이아웃 문법을 발명하지 않고 이 검증된 구조를 그대로 채택한다. 다만 Discord 특유의 게이머 지향 톤(네온, 과한 컬러, 게임화 요소)은 배제하고 Slack에 가까운 업무 도구 톤을 취한다.
- **Linear (정밀한 미니멀 다크 시스템)**: 헤어라인급 얇은 보더(0.5px), 낮은 폰트 굵기(400~510), 그림자 대신 "밝은 표면이 어두운 배경 위에 얹히는" 방식의 다크모드 위계, 단일 액센트 컬러를 절제해서 쓰는 태도를 채택한다. 소규모 개발팀이 주 사용자층이라는 점에서 Linear식 "개발자 도구 다크모드 우선" 감성이 톤에 부합한다.
- **2026 SaaS 대시보드 트렌드 (Strategic Minimalism)**: 우측 "안 읽은 정보 대시보드"는 정보를 다 나열하지 않고 5~9개 내외로 우선순위를 정리해 보여주는 최신 대시보드 트렌드를 따른다. 차분한 뉴트럴 톤 + 단일 포인트 컬러로 데이터를 강조하는 방식을 채택한다.

이 프로젝트의 톤앤매너는 **명확성 > 트렌디함**이다. 화려한 그라디언트, 글래스모피즘, 과도한 모션은 지양하고, 정보 위계와 상태(접속중/오프라인, 호스트/참가자, 읽음/안읽음)를 즉각적으로 구분할 수 있게 하는 데 모든 디자인 결정의 우선순위를 둔다.

---

## 2. 컬러 팔레트

### 2.1 브랜드 컬러

| 역할 | 이름 | Hex | 용도 |
|---|---|---|---|
| Primary | Indigo 500 | `#4F5FF0` | 주요 버튼, 링크, 활성 탭, 포커스 링, 선택된 채팅방 |
| Primary (hover) | Indigo 600 | `#3E4CD1` | Primary 버튼 hover/active |
| Primary (light tint) | Indigo 50 | `#EEF0FE` | 선택된 리스트 아이템 배경, 연한 하이라이트 |
| Secondary | Slate 900 | `#111318` | 다크모드 기본 배경 |
| Secondary | Slate 800 | `#191B22` | 다크모드 표면(사이드바/패널) |
| Secondary | Slate 50 | `#F7F8FA` | 라이트모드 기본 배경 |
| Secondary | White | `#FFFFFF` | 라이트모드 표면(카드/패널) |
| Accent | Amber 500 | `#F2B84B` | 호스트 배지, 공지(호스트 전용 채팅방) 강조, 안읽음 뱃지 |

### 2.2 시맨틱 컬러 (상태 전용, 브랜드 컬러와 절대 혼용하지 않음)

| 상태 | Hex | 용도 |
|---|---|---|
| 온라인 | `#2FBF6E` | 접속 상태 점, 참가자 목록 |
| 자리비움/유휴 | `#F2B84B` (Amber 재사용) | 5분 이상 미활동 시 (선택 구현) |
| 오프라인 | `#8A8F9B` | 접속 상태 점, 회색 처리된 아바타 |
| 위험/종료 | `#E5484D` | 회의방 종료 버튼, 삭제, 정원 초과 오류 |
| 성공 | `#2FBF6E` | 참가 성공, 코드 복사 완료 토스트 |

### 2.3 채팅방 유형 태그 컬러 (좌측 목록 아이콘/뱃지 전용)

| 채팅방 유형 | 컬러 | 아이콘 모티프 |
|---|---|---|
| 일반 채팅방 | Slate 500 `#6B7280` (중립) | `#` |
| 호스트 전용(공지) | Amber `#F2B84B` | 메가폰 |
| 코드 에디터 | Teal `#14B8A6` | `</>` |

세 가지 유형이 목록에서 즉시 구분되도록 색상+아이콘을 이중으로 사용한다(색약 사용자 대응, 7장 참조).

### 2.4 텍스트 컬러

| 용도 | 라이트모드 | 다크모드 |
|---|---|---|
| 본문 강조 | `#12141A` | `#F2F3F5` |
| 본문 기본 | `#3C3F47` | `#C7CAD1` |
| 보조/메타(타임스탬프 등) | `#8A8F9B` | `#7B7F8A` |
| 비활성/placeholder | `#B4B8C0` | `#565A63` |

라이트모드를 기본값으로 하되 다크모드를 동등한 완성도로 지원한다(개발자 사용자 다수 고려, Linear 사례 참고). 색상 값은 CSS 커스텀 프로퍼티(`--color-*`)로 정의해 테마 전환 시 토큰만 교체하는 구조를 권장한다.

---

## 3. 타이포그래피

### 3.1 폰트 패밀리
- **UI 전체(한글/영문/숫자)**: `Pretendard` (Variable) — 오픈소스, CDN/npm으로 배포되어 별도 라이선스 다운로드 없이 사용 가능. 한글 가독성과 영문/숫자 혼용 시 균형이 좋아 2024~2026년 국내 SaaS UI 표준으로 자리잡음. 폴백: `-apple-system, "Segoe UI", Roboto, sans-serif`.
- **코드 에디터 전용(monospace)**: `JetBrains Mono` — 오픈소스, Google Fonts/CDN 제공. 폴백: `"D2Coding", Consolas, "Courier New", monospace`.

> 위 두 폰트 모두 오픈소스이며 CDN(`@font-face` 또는 웹폰트 링크) 또는 패키지 매니저로 로드하므로, 사용자가 별도로 폰트 파일을 구매/다운로드할 필요가 없다. 이 방식으로 진행하며, 만약 개발 단계에서 다른 유료/라이선스 폰트가 필요해지면 그때 다시 확인한다.

### 3.2 타입 스케일

| 이름 | 크기 | Weight | 용도 |
|---|---|---|---|
| Display | 28px / line-height 36px | 700 | 랜딩/로그인 헤드라인 |
| H1 | 22px / 30px | 700 | 페이지 타이틀 (회의방 이름 등) |
| H2 | 18px / 26px | 600 | 섹션 타이틀 (설정 패널, 대시보드 헤더) |
| H3 | 15px / 22px | 600 | 채팅방 목록 그룹명, 카드 타이틀 |
| Body | 14px / 20px | 400 | 채팅 메시지, 일반 본문 |
| Body Strong | 14px / 20px | 600 | 발신자 이름, 강조 텍스트 |
| Caption | 12px / 16px | 400 | 타임스탬프, 메타 정보, 헬퍼 텍스트 |
| Code | 14px / 22px | 400 (monospace) | 코드 에디터 본문 |
| Button | 14px / 20px | 600 | 버튼 라벨 |

- 본문 최소 크기는 14px, 메타 정보라도 12px 미만으로 내려가지 않는다(7장 접근성 참고).
- Weight는 400/600/700 세 단계만 사용해 과도한 굵기 변주를 피한다(Linear식 절제).

---

## 4. 레이아웃 원칙

### 4.1 전체 구조 (데스크톱 기준, PRD 4.3 화면 구조 반영)

```
┌───────────────────────────────────────────────────────────────┐
│  Top Bar (56px)  : 회의방 이름 · 참가자 아바타 목록 + 접속상태   │
├───────────┬───────────────────────────────────┬───────────────┤
│           │                                   │               │
│ Left Nav  │           Main Content             │  Right        │
│ (272px)   │           (Flexible, min 480px)    │  Dashboard    │
│ 채팅방 목록│                                    │  (320px)      │
│           │                                   │               │
├───────────┤                                   │               │
│ Settings  │                                   │               │
│ (하단고정) │                                   │               │
└───────────┴───────────────────────────────────┴───────────────┘
```

- **Top Bar**: 높이 56px 고정. 좌측 회의방명, 우측 참가자 아바타 스택(최대 10명, 온라인 우선 정렬) + 접속 상태 점.
- **Left Nav**: 폭 272px 고정(리사이즈 불가, 접기 가능 토글 제공). 상단에 채팅방 목록(호스트만 "+생성" 버튼 노출), 최하단에 설정 버튼 고정 배치.
- **Main Content**: 가변 폭, 최소 480px 미만으로 좁아지면 우측 대시보드가 자동으로 오버레이 전환(4.3 참조).
- **Right Dashboard**: 폭 320px 고정, 접기/펼치기 가능. 기본은 펼침 상태.

### 4.2 그리드 & 여백
- 기본 여백 단위: **4px 배수 스케일** (4 / 8 / 12 / 16 / 24 / 32 / 48).
- 패널 내부 패딩: 16px(컴팩트 영역) ~ 24px(주요 컨텐츠 영역).
- 리스트 아이템 간 간격: 4px, 그룹 간 간격: 16px.
- 카드/패널 모서리 반경: 8px(버튼/입력) ~ 12px(카드/모달).
- 보더: 1px hairline, 라이트모드 `#E5E7EB` / 다크모드 `#2A2D36` — 그림자보다 보더로 위계를 표현(Linear식).

### 4.3 반응형 기준

| Breakpoint | 범위 | 레이아웃 동작 |
|---|---|---|
| Desktop | ≥ 1280px | 4구역(Top/Left/Main/Right) 동시 노출 |
| Tablet | 768px ~ 1279px | Right Dashboard가 아이콘 토글로 접히고, 클릭 시 Main 위에 오버레이(Drawer)로 표시 |
| Mobile | < 768px | Left Nav와 Right Dashboard 모두 숨김. 하단 탭바(채팅목록 / 메인 / 대시보드 / 설정) 방식으로 전환해 한 번에 하나의 영역만 전체 화면 표시 |

회의방 도구의 주 사용 맥락은 데스크톱이지만, 참가자가 이동 중 알림만 빠르게 확인하는 시나리오를 고려해 모바일에서도 핵심 플로우(메시지 확인/전송, 대시보드 확인)는 완전히 동작해야 한다. 코드 에디터는 모바일에서 편집은 가능하되 "열람 우선" UX로 안내(가로 스크롤 허용, 축소된 툴바)한다.

---

## 5. 주요 컴포넌트 스펙

### 5.1 버튼

| 종류 | 배경 | 텍스트 | 보더 | 용도 |
|---|---|---|---|---|
| Primary | `#4F5FF0` | White | none | 회의방 생성, 참가, 메시지 전송, 확인 |
| Secondary | transparent | Primary 텍스트 색 | 1px `#4F5FF0` | 취소, 보조 액션 |
| Ghost | transparent | 본문 텍스트 색 | none | 아이콘 버튼, 리스트 내 인라인 액션 |
| Danger | `#E5484D` | White | none | 회의방 종료, 채팅방 삭제 |

**상태별 스타일**
- Default → Hover: 배경 8% 어둡게(Primary/Danger) 혹은 Indigo 50 배경 적용(Secondary/Ghost).
- Active(누르는 중): 배경 12% 어둡게 + scale(0.98) 적용.
- Focus-visible: 2px outline, offset 2px, 색상은 Primary(다크모드는 밝은 Indigo 300).
- Disabled: opacity 0.4, cursor not-allowed, 클릭 이벤트 없음.
- 최소 높이 36px(컴팩트), 40px(기본), 최소 터치 영역 40x40px 확보(아이콘 버튼 포함).

### 5.2 카드 / 리스트 아이템 (채팅방 목록, 참가자 목록, 대시보드 항목)

- 기본 상태: 배경 투명, 텍스트 기본색.
- Hover: 배경 `rgba(0,0,0,0.04)`(라이트) / `rgba(255,255,255,0.06)`(다크).
- 선택됨(현재 열려있는 채팅방): 배경 Indigo 50 / 다크모드는 Indigo 500의 12% 오퍼시티, 좌측에 3px Primary 색 인디케이터 바.
- 안읽음 상태: 항목명 Bold 처리 + 우측에 Amber 원형 카운트 뱃지(최대 표시 "99+").
- 채팅방 유형 아이콘: 2.3절 컬러/아이콘 매핑 적용.
- 호스트 전용 채팅방 목록 항목에는 작은 "호스트" Amber 배지 텍스트 병기(참가자 화면에서 "공지"로 표기).

### 5.3 참가자 목록 / 아바타

- 아바타: 32px(리스트) / 24px(탑바 스택) 원형, 이니셜 또는 프로필 이미지.
- 접속 상태 점: 아바타 우하단에 8px 원, 온라인(`#2FBF6E`)/오프라인(`#8A8F9B`), 흰색/배경색 2px 테두리로 분리.
- 호스트 표시: 아바타 좌상단에 작은 왕관 아이콘(Amber), 리스트에서는 이름 옆 "호스트" 텍스트 배지 병기.
- 탑바에서는 최대 5개 아바타 노출 후 "+N" 카운트로 축약, 클릭 시 전체 목록 팝오버.

### 5.4 입력 폼

- 기본 텍스트 인풋: 높이 40px, 패딩 좌우 12px, 보더 1px `#E5E7EB`(다크 `#2A2D36`), radius 8px.
- Focus: 보더 색 Primary로 전환 + 2px Primary 20% 오퍼시티 outer glow.
- Error: 보더 색 Danger, 하단에 12px Danger 텍스트로 에러 메시지(예: "정원이 초과된 회의방입니다", "존재하지 않는 코드입니다").
- 6자리 코드 입력: 6개의 개별 박스(각 40x48px)로 분리 입력 UI 채택(OTP 스타일) — 코드 성격을 시각적으로 강조하고 오타를 줄임.
- 메시지 입력창(채팅): 하단 고정, 여러 줄 자동 확장(최대 6줄), Enter 전송 / Shift+Enter 줄바꿈.
- 호스트 전용 채팅방에서 일반 참가자는 입력창 대신 "리액션 바"(이모지 6종 고정 세트 + 더보기)만 노출.

### 5.5 채팅 메시지 버블

- 내 메시지: 우측 정렬, 배경 Primary Indigo 50(라이트) / Indigo 500 18%(다크), radius 12px(말풍선 꼬리 없음, 미니멀).
- 상대 메시지: 좌측 정렬, 배경 표면색과 동일 + 1px 보더, 발신자 아바타(28px) + 이름(Body Strong) + 타임스탬프(Caption) 병기.
- 연속 메시지(같은 발신자, 1분 이내): 아바타/이름 생략하고 여백만 축소(4px)해 밀도 확보.
- 공지(호스트 전용) 메시지: 좌측에 4px Amber 바 + "공지" Caption 라벨 추가, 리액션은 메시지 하단에 이모지+카운트 pill 형태로 표시.

### 5.6 코드 에디터

- 컨테이너: 좌측 라인 넘버(Caption 크기, 저대비 텍스트), 상단 툴바(언어 선택 드롭다운, 참가자 커서 색상 범례, 저장 상태 인디케이터).
- 문법 강조 테마: 라이트모드는 GitHub Light 유사 톤, 다크모드는 One Dark 유사 톤 채택(개발자에게 익숙한 배색 유지).
- 참가자별 커서: 브랜드 컬러와 겹치지 않는 고정 6색 팔레트(예: `#F97316`, `#22C55E`, `#3B82F6`, `#EC4899`, `#A855F7`, `#14B8A6`)를 참가자 접속 순서로 순환 할당, 커서 상단에 이름 라벨 pill 표시.
- 언어 선택: 드롭다운(JavaScript / Python / HTML·CSS / JSON / Markdown 등), 선택 즉시 하이라이팅 갱신.

### 5.7 모달 / 다이얼로그

- 배경 오버레이: `rgba(0, 0, 0, 0.4)`.
- 모달 카드: 최대 폭 420px(확인창)~560px(생성 폼), radius 12px, 그림자 `0 8px 24px rgba(0,0,0,0.12)`(라이트) — 다크모드는 그림자 대신 1px 밝은 보더로 대체.
- 위험 액션(회의방 종료, 채팅방 삭제)은 반드시 2단계 확인(체크박스 또는 이름 재입력 없이, 명확한 경고 문구 + Danger 버튼 재확인)으로 처리.

### 5.8 설정 패널 (좌측 하단)

- 좌측 하단 아이콘 버튼 클릭 시 팝오버/사이드시트로 확장.
- 공통 항목: 회의방 상태(진행중/종료됨 뱃지), 6자리 코드(복사 버튼 포함, 클릭 시 "복사됨" 토스트).
- 호스트 전용 추가 항목: "호스트 권한 위임"(참가자 선택 리스트 → 확인), "회의방 종료"(Danger 버튼, 5.7 확인 모달 연결).

### 5.9 안 읽은 정보 대시보드 (우측)

- 상단에 "전체 읽음 처리" Ghost 버튼(우측 정렬).
- 항목 그룹: 새 메시지 / 코드 변경 / 호스트 공지 3개 섹션(2026 대시보드 트렌드에 따라 카테고리당 상위 몇 건만 기본 노출, "더보기"로 확장).
- 각 항목: 출처 채팅방 유형 아이콘(2.3절 컬러) + 미리보기 텍스트(1줄 truncate) + 상대 시각(Caption).
- 안읽음 항목은 좌측 3px Amber 인디케이터 바 + 배경 미세 강조, 클릭 시 해당 채팅방 이동과 동시에 읽음 처리(회색 처리로 즉시 피드백).

---

## 6. 인터랙션 / 애니메이션 가이드

애니메이션은 "상태 변화를 인지시키는 최소한의 신호"로만 사용하고, 장식적 모션은 배제한다.

- **트랜지션 시간**: 120~200ms, easing `ease-out` 기본. 그 이상 지속되는 애니메이션은 사용하지 않는다.
- **리스트/패널 열림·닫힘**: 8px 이내의 짧은 slide + fade (opacity 0→1, translateY 4px→0).
- **새 메시지 도착**: 채팅 리스트 하단에 fade-in만 적용(bounce, scale 등 과장된 효과 금지).
- **대시보드 신규 항목**: 좌측 인디케이터 바가 순간적으로 강조된 후(1회 pulse, 300ms) 일반 상태로 정착.
- **접속 상태 점**: 오프라인→온라인 전환 시 0.2초 컬러 트랜지션만 적용, 지속적 pulse 애니메이션은 재연결 시도 중일 때만 사용.
- **토스트(코드 복사, 참가 성공 등)**: 우측 하단 등장, 2.5초 후 자동 소실, hover 시 유지.
- **로딩 상태**: 스피너 대신 스켈레톤(뉴트럴 회색 블록 pulse)을 우선 사용해 레이아웃 시프트를 방지.
- `prefers-reduced-motion: reduce` 설정 시 모든 트랜지션/펄스 애니메이션을 즉시 전환(0ms)으로 대체한다.

---

## 7. 접근성 고려사항

- **명도 대비**: 본문 텍스트 대비율 최소 4.5:1(WCAG AA), 큰 텍스트(18px bold 이상)는 3:1 이상 확보. Amber(`#F2B84B`) 위에 흰색 텍스트를 올리지 않고, 반드시 어두운 텍스트(`#12141A`)와 조합해 대비를 확보한다.
- **색상 단독 의존 금지**: 접속 상태(온라인/오프라인), 채팅방 유형, 호스트 여부는 색상과 함께 아이콘·텍스트 라벨을 병기해 색약 사용자도 구분 가능하게 한다.
- **폰트 크기**: 본문 최소 14px, 메타 정보 최소 12px 유지. 사용자 브라우저 확대(최대 200%) 시 레이아웃 깨짐 없이 반응형으로 대응.
- **포커스 관리**: 모든 인터랙션 요소에 키보드 포커스 가능 + focus-visible 스타일 필수. 모달 오픈 시 포커스 트랩 적용, ESC로 닫기 지원.
- **터치 타겟**: 최소 40x40px, 리스트 아이템 클릭 영역은 아이콘뿐 아니라 행 전체로 확장.
- **스크린 리더 대응**: 접속 상태 점, 안읽음 뱃지, 호스트 배지에는 `aria-label`(예: "온라인", "안읽은 메시지 3개", "호스트")을 제공. 대시보드 신규 알림 영역은 `aria-live="polite"`로 실시간 갱신을 안내.
- **키보드 네비게이션**: 채팅방 목록/참가자 목록은 방향키 이동 가능, 코드 에디터는 표준 텍스트 편집 키 조작을 그대로 지원(에디터 라이브러리의 기본 접근성 기능 유지).
- **다크모드 대비 검증**: 다크모드 표면 위 텍스트/보더 대비도 라이트모드와 동일한 AA 기준을 별도로 검증한다(단순 색 반전 금지).

---

(본 문서에서 정의한 컬러 Hex, 폰트, 애니메이션 수치는 개발자 서브에이전트가 CSS 변수/디자인 토큰으로 그대로 구현하는 것을 전제로 한다. PRD 범위를 벗어나는 화면 구조 변경이 필요할 경우 사용자에게 먼저 확인한다.)
