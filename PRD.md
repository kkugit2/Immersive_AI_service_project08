# PRD: 팀 소통 협업 웹앱 (TeamRoom)

## 1. 프로젝트 개요

TeamRoom은 3~10명 규모의 소규모 팀이 하나의 "회의방(Room)" 안에서 실시간으로 소통하고 협업할 수 있도록 돕는 웹앱이다. 대규모 조직용 협업 툴은 기능이 과도하게 많고 설정이 복잡한 반면, 소규모 팀은 빠르게 방을 만들고 코드 하나로 팀원을 초대해 즉시 대화·공지·협업 코딩을 시작할 수 있는 가볍고 명확한 도구를 필요로 한다. 이메일 기반 회원가입만으로 누구나 서비스를 이용할 수 있으며, 로그인 후 호스트로서 새 회의방을 만들거나 6자리 고유번호로 기존 회의방에 참가할 수 있다. 회의방 내부에서는 목적에 따라 일반 채팅방, 호스트 전용 공지 채팅방, 실시간 협업 코드 에디터를 자유롭게 만들어 사용하며, 우측 대시보드를 통해 놓친 소식을 한눈에 파악할 수 있다. 본 프로젝트의 목표는 소규모 팀의 일상적인 소통·공지·경량 페어 프로그래밍 니즈를 하나의 화면에서 해결하는 것이다.

## 2. 타깃 사용자 분석

**페르소나 1: 호스트 - 스타트업 팀 리드 "지민"**
- 5~8명 규모의 개발팀을 이끄는 팀 리드로, 매일 팀원들과 진행상황을 공유하고 공지를 전달해야 한다.
- 니즈: 팀원을 빠르게 초대(6자리 코드 공유)하고, 회의방 안에서 공지용 채널과 자유 대화 채널을 구분하고 싶다. 필요 시 자신이 자리를 비울 때 다른 팀원에게 방장 권한을 넘기고 싶다.
- 니즈: 팀원 각각이 지금 접속해 있는지, 새로 들어온 팀원이 있는지 한눈에 확인하고 싶다.

**페르소나 2: 참가자 - 주니어 개발자 "서연"**
- 여러 소규모 프로젝트 방에 동시에 소속되어 있으며, 각 방에서 오가는 대화와 코드 변경을 놓치지 않고 따라가야 한다.
- 니즈: 잠깐 자리를 비운 사이 쌓인 새 메시지·코드 변경·공지사항을 대시보드에서 빠르게 확인하고 싶다.
- 니즈: 팀원과 같은 코드를 동시에 열어 실시간으로 함께 수정하며 아이디어를 검증하고 싶다.

## 3. 핵심 기능 목록

| 기능명 | 설명 | 우선순위 |
|---|---|---|
| 이메일 기반 회원가입/로그인 | 이메일·비밀번호로 가입 후 로그인하여 서비스 이용 | P0 |
| 회의방 생성(호스트) | 로그인 후 새 회의방을 만들면 자동으로 호스트가 되고 6자리 고유번호가 발급됨 | P0 |
| 회의방 참가(6자리 코드) | 6자리 고유번호 입력으로 기존 회의방에 참가 (정원 10명 초과 시 참가 불가) | P0 |
| 참가자 목록 및 실시간 접속 상태 | 회의방 상단에 소속 인원과 온라인/오프라인 상태를 실시간 표시 | P0 |
| 채팅방 생성/관리(호스트 전용) | 좌측 패널에서 일반/호스트전용/코드에디터 채팅방을 호스트만 생성·삭제·이름변경 가능 | P0 |
| 일반 채팅방 | 회의방 소속 누구나 자유롭게 메시지를 주고받는 채널 | P0 |
| 호스트 전용 채팅방(공지) | 호스트만 메시지 작성 가능, 참가자는 이모지 리액션만 가능 | P0 |
| 실시간 협업 코드 에디터 | 여러 참가자가 동시에 같은 코드 문서를 실시간 공동 편집, 문법 강조 지원 | P0 |
| 설정 패널(좌측 하단) | 현재 회의방 상태(진행중/종료 등)와 6자리 고유번호 확인, 호스트는 회의방 종료 가능 | P0 |
| 안 읽은 정보 대시보드(우측) | 새 메시지, 코드 에디터 변경사항, 호스트 공지를 모아 보여주는 알림 피드 | P0 |
| 호스트 권한 이관 | 호스트가 특정 참가자에게 권한을 위임하거나, 호스트가 나가면 자동으로 이관 | P1 |
| 회의방 종료/삭제 | 호스트가 회의방을 명시적으로 종료하면 회의방과 모든 채팅 기록이 삭제됨 | P1 |
| 이모지 리액션 | 호스트 전용 채팅방 메시지에 이모지로 반응 | P1 |
| 코드 에디터 언어 선택 및 문법 강조 | 주요 언어(JavaScript, Python, HTML/CSS, JSON, Markdown 등) 선택 및 하이라이팅 | P1 |
| 대시보드 읽음 처리 | 알림 항목 클릭 시/전체 읽음 처리 | P2 |
| 회의방 나가기(참가자) | 참가자가 스스로 회의방에서 나가기 | P2 |

## 4. 사용자 플로우

### 4.1 회원가입 및 로그인
1. 사용자가 랜딩 페이지에서 "회원가입"을 선택하고 이메일·비밀번호를 입력한다.
2. 이메일 인증(가정: Supabase Auth 표준 이메일 인증 절차 사용) 완료 후 로그인 가능 상태가 된다.
3. 로그인하면 "홈" 화면으로 이동한다.

### 4.2 홈 화면에서 회의방 시작하기
1. 홈 화면에서 "새 회의방 만들기" 또는 "회의방 참가하기" 중 하나를 선택한다.
2. **호스트가 되는 경우**: 회의방 이름을 입력 → 회의방이 생성되고 6자리 고유번호가 발급됨 → 즉시 회의방 화면으로 이동, 자동으로 host 역할 부여.
3. **참가하는 경우**: 6자리 고유번호 입력 → 유효성 확인(존재 여부, 정원 10명 이하 여부) → 성공 시 회의방 화면으로 이동, member 역할 부여. 정원 초과 또는 존재하지 않는 코드일 경우 오류 메시지 표시.

### 4.3 회의방 화면 사용
1. 회의방에 입장하면 상단에서 현재 소속 인원과 각자의 실시간 접속 상태(온라인/오프라인)를 확인한다.
2. 좌측 채팅방 목록에서 원하는 채팅방(일반/호스트전용/코드에디터)을 선택하면 중앙 메인 영역에 해당 콘텐츠가 표시된다.
   - 일반 채팅방 선택 시: 메시지 입력창과 대화 내역이 표시되고 누구나 작성 가능.
   - 호스트 전용 채팅방 선택 시: 호스트에게는 메시지 입력창이, 참가자에게는 리액션 버튼만 표시.
   - 코드 에디터 선택 시: 실시간 공동 편집 가능한 코드 에디터가 표시되고, 참가자별 커서 위치가 구분되어 표시됨(가정).
3. 호스트는 좌측 채팅방 목록 상단의 "+" 버튼으로 새 채팅방을 만들고 유형을 선택한다(일반/호스트전용/코드에디터). 참가자에게는 생성/삭제/관리 버튼이 보이지 않는다.
4. 우측 대시보드에서 자신이 아직 확인하지 않은 새 메시지, 코드 에디터 변경사항, 호스트 공지를 리스트로 확인하고 클릭 시 해당 채팅방으로 이동 및 읽음 처리된다.
5. 좌측 하단 설정 버튼을 누르면 현재 회의방 상태와 6자리 고유번호를 보여주는 패널이 열린다. 호스트에게는 여기서 "호스트 권한 위임"과 "회의방 종료" 옵션이 추가로 노출된다.

### 4.4 호스트 권한 이관 및 회의방 종료
1. 호스트가 설정 패널에서 "호스트 권한 위임"을 선택하고 대상 참가자를 지정하면, 해당 참가자가 즉시 새 호스트가 되고 기존 호스트는 일반 참가자가 된다.
2. 호스트가 별도로 위임하지 않고 회의방을 나가는 경우, (가정: 참가 순서가 가장 빠른 참가자에게) 자동으로 호스트 권한이 이관된다.
3. 호스트가 설정 패널에서 "회의방 종료"를 선택하면 확인 절차 후 회의방과 그 안의 모든 채팅방, 메시지, 코드 문서가 삭제되고, 모든 참가자는 홈 화면으로 이동된다.

## 5. 기술 스택

- HTML
- CSS
- JavaScript

(가정: 실시간 접속 상태, 채팅, 코드 공동편집의 실시간 동기화는 Supabase Realtime 채널을 기반으로 순수 HTML/CSS/JavaScript에서 직접 구현하는 것으로 가정함. 코드 에디터의 동시 편집 충돌 처리는 클라이언트 측 경량 동시편집 로직으로 구현하며, 별도 프레임워크 도입 여부는 개발 단계에서 구체화한다.)

## 6. 사용 MCP

- **Supabase**: 사용자 인증(이메일 회원가입/로그인), 회의방/채팅방/메시지/코드 문서 등 데이터 저장, Realtime 기능을 통한 접속 상태·메시지·코드 편집 실시간 동기화
- **Vercel**: 프론트엔드 배포 및 호스팅

## 7. 데이터 구조

### 7.1 핵심 엔티티

**users** (Supabase Auth 관리 + 프로필 확장)
- id (PK)
- email
- display_name
- created_at

**meeting_rooms** (회의방)
- id (PK)
- room_code (6자리 고유번호, unique)
- name (회의방 이름)
- host_user_id (FK → users.id, 현재 호스트)
- status (active / ended)
- created_at
- ended_at (nullable)

**room_members** (회의방 소속 및 역할)
- id (PK)
- room_id (FK → meeting_rooms.id)
- user_id (FK → users.id)
- role (host / member)
- joined_at
- last_seen_at (실시간 접속 상태 판단용, 가정: Realtime presence와 병행 사용)

**chat_rooms** (회의방 내 채팅방/코드에디터)
- id (PK)
- room_id (FK → meeting_rooms.id)
- type (general / host_only / code_editor)
- name
- created_by (FK → users.id, 호스트만 가능)
- created_at

**messages** (일반 채팅방·호스트 전용 채팅방의 메시지)
- id (PK)
- chat_room_id (FK → chat_rooms.id)
- sender_id (FK → users.id)
- content
- created_at

**reactions** (호스트 전용 채팅방 메시지에 대한 이모지 반응)
- id (PK)
- message_id (FK → messages.id)
- user_id (FK → users.id)
- emoji
- created_at

**code_documents** (코드 에디터 유형 채팅방의 문서, chat_rooms와 1:1)
- id (PK)
- chat_room_id (FK → chat_rooms.id, unique)
- language (문법 강조용 언어 설정)
- content (현재 코드 스냅샷)
- updated_at

**notifications** (우측 대시보드용 안 읽은 정보)
- id (PK)
- user_id (FK → users.id, 수신자)
- room_id (FK → meeting_rooms.id)
- chat_room_id (FK → chat_rooms.id, 출처)
- type (new_message / code_edit / host_announcement)
- preview_text
- is_read (boolean)
- created_at

### 7.2 관계 요약
- 한 user는 여러 meeting_rooms에 room_members를 통해 동시에 소속될 수 있다(가정).
- 하나의 meeting_room은 여러 chat_rooms(일반/호스트전용/코드에디터 혼합)를 가진다.
- 하나의 chat_room(general, host_only)은 여러 messages를 가지며, 하나의 message는 여러 reactions를 가질 수 있다.
- 하나의 chat_room(code_editor 타입)은 하나의 code_documents 레코드와 1:1로 연결된다.
- meeting_room 삭제(종료) 시 하위 chat_rooms, messages, reactions, code_documents, notifications가 연쇄 삭제된다(cascade).

## 8. 개발 우선순위

### MVP 범위 (1차 출시)
- 이메일 회원가입/로그인
- 회의방 생성(호스트) 및 6자리 코드로 참가(정원 10명 제한)
- 회의방 상단 참가자 목록 및 실시간 접속 상태 표시
- 채팅방 생성/관리(호스트 전용): 일반 채팅방, 호스트 전용 채팅방
- 일반 채팅방 실시간 메시지 송수신
- 호스트 전용 채팅방: 호스트 메시지 작성 + 참가자 이모지 리액션
- 코드 에디터 채팅방: 실시간 공동 편집(기본 동시 편집) + 문법 강조
- 좌측 하단 설정 패널(회의방 상태·고유번호 확인, 호스트의 회의방 종료)
- 우측 대시보드(새 메시지, 코드 변경, 호스트 공지 알림)

### 이후 단계 (2차 이후)
- 호스트 권한 수동 위임 및 호스트 이탈 시 자동 이관 로직 고도화
- 코드 에디터 참가자별 커서/선택 영역 실시간 표시, 편집 충돌 최소화 고도화
- 대시보드 알림 개별/전체 읽음 처리 UX 개선
- 참가자 자발적 회의방 나가기
- 코드 에디터 지원 언어 확장 및 코드 스냅샷 히스토리(버전 관리)
- 채팅 메시지 검색, 읽음 확인 등 부가 기능

---
(본 문서에서 "가정"으로 표시된 항목은 사용자 요청에 명시되지 않아 소규모 팀 협업 도구의 맥락에 맞게 합리적으로 구체화한 내용이며, 추후 확인/변경될 수 있음.)
